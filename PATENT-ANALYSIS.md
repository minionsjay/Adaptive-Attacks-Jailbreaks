# AMS-RedTeam 可申请专利的创新点分析

> 基于当前系统实现，从"技术方案""创新性""权利要求"三个维度分析。
> 建议申请类型：**发明专利**（方法+系统），可拆分为 1~2 件独立申请。

---

## 创新点总览（按专利价值排序）

### ★★★ 创新点 1：多维度自适应攻击进化方法与系统

**对应现有技术**：论文使用了 Gemini 2.5 Pro + MAP-Elites（长度×多样性二维度）
**我们的改进**：在 MAP-Elites 基础上引入**语言/书写变体维度**作为第三进化方向，
形成"长度 × 多样性 × 语言变体"的三维行为空间

**技术方案**：
- 15 种语言/书写变体（拼音、缩写、谐音、emoji、混合语、繁简体、低资源语言…）
- 行为描述子扩展为三元组 (length_bin, diversity_bin, language_variant)
- MAP-Elites 网格按语言变体维度分区，每区独立进化
- 攻击者模型接收"未测试语言优先"指令，自动覆盖语言盲区

**为什么可以申请**：论文只用英文，我们的多语言进化是**新的技术特征**，
且实验数据证明它能找到单语言攻击找不到的盲区（拼音绕过率 100%）

**权利要求示例**：
```
1. 一种基于多语言变异的LLM安全防御自适应评估方法，其特征在于包括：
   a) 将攻击候选按文本长度、语义多样性、语言变体三个行为维度
      映射至MAP-Elites行为网格；
   b) 对每个网格单元保留适应度最高的攻击候选作为精英；
   c) 基于当前全局最优、随机精英及邻近网格精英生成灵感组合；
   d) 将灵感组合与防御系统反馈注入大语言模型生成下一代攻击候选；
   e) 重复步骤a-d直至满足终止条件。
```

---

### ★★★ 创新点 2：双层判定机制（Critic-Oracle分离 + 可操作性校验）

**对应现有技术**：论文用 critic 引导搜索、用 benchmark ground-truth 判定成功，
但**没有**可操作性校验这一步

**我们的改进**：在 LLM Judge 判定之后增加**自动可操作性校验层**，
通过检测回复中是否包含"可直接用于实施犯罪的指令/代码/模板"来过滤假阳性

**技术方案**：
- 第一层：LLM Judge 输出六档判定（Y/YP/YD/NI/NR/NC）
- 第二层：可操作性检查器扫描回复中的**可操作内容特征**
  （代码结构、逐步指令、完整模板、合成步骤、话术脚本等）
- 若 Judge 判 Y 但可操作性检查判 False → 自动降级为 NR（过滤假阳性）
- 若检查不确定 → 保留 Judge 原判定

**为什么可以申请**：论文承认"LLM裁判会被操纵"（教训④），但**没有给出解决
方案**。我们的双层判定是对这个问题的**具体技术解决**。

**权利要求示例**：
```
2. 根据权利要求1所述的方法，其特征在于所述评分步骤包括双层判定：
   a) 由第一判定模型对攻击响应进行初步分类，输出六档判定结果；
   b) 当初步分类为有害时，由可操作性校验模块对响应文本进行
      可操作内容特征匹配，所述特征包括：可执行代码结构、
      逐步犯罪指令、完整钓鱼模板、化学合成步骤；
   c) 当可操作性校验判定为否时，将判定结果降级为非有害。
```

---

### ★★☆ 创新点 3：攻防闭环数据回流系统

**对应现有技术**：论文提到了"对抗训练"的概念但**没有实现数据回流管道**

**我们的改进**：自动将进化产生的绕过样本转换为**结构化训练数据**，
形成"攻击→检测→漏报→训练→检测器升级→再攻击"的闭环

**技术方案**：
- 绕过样本（Guard放行+victim违规）→ 标注为正例（漏报）
- 已拦截样本（Guard拦住+攻击成功）→ 标注为已检测正例
- 良性样本（Guard放行+victim拒绝）→ 标注为负例（防误报）
- 导出为 JSONL 格式，直接用于检测器微调/LoRA训练

**为什么可以申请**：形成了一个**自动化的检测器持续改进系统**，
不需要人工收集攻击样本。

**权利要求示例**：
```
3. 一种基于自适应攻击的LLM安全检测器持续改进方法，其特征在于包括：
   a) 通过权利要求1所述方法生成攻击候选并评估；
   b) 将绕过防御的攻击样本标注为正例训练数据；
   c) 将被拦截的攻击样本标注为已检测正例训练数据；
   d) 将被拒绝的良性样本标注为负例训练数据；
   e) 使用步骤b-d的训练数据对安全检测模型进行增量训练；
   f) 使用更新后的检测模型重复步骤a-e，形成持续改进闭环。
```

---

### ★★☆ 创新点 4：基于共识筛选的防御鲁棒性评估方法

**来源**：用户（你）提出的实验设计

**技术方案**：
- 预筛选：将种子攻击集通过 N 个安全检测模型，筛选出被 **≥K 个模型
  同时拦截** 的种子子集（"共识可检测集"）
- 进化攻击：仅使用共识可检测集作为进化起点
- 鲁棒性度量：进化后各模型的拦截率下降幅度 = 防御鲁棒性得分
- 优势：排除了"因种子太弱导致的假阴性"，直接测试防御在
  "已知可检测攻击"面对变异时的真实抵抗力

**为什么可以申请**：这是一种**新的安全评估方法论**，排除了传统方法
中"种子质量不均导致的评估偏差"。

---

### ★☆☆ 创新点 5：自校准困惑度检测器

**技术方案**：
- 使用小语言模型（如 Qwen2.5-1.5B）对输入文本进行滑动窗口困惑度计算
- 启动时自动用内置多语言良性语料校准阈值（良性最大PPL × 安全边际系数）
- 无需人工设定阈值，自动适配不同语言/模型/场景

**为什么可以申请**：解决了固定阈值不可移植的问题（论文的6.031在
中文场景下会全量误报）

---

## 专利申请建议

### 方案一：一件大专利（整体系统）

**标题**：一种基于多语言自适应进化的LLM安全防御评估方法与系统

**权利要求结构**：
1. 独立权利要求1：四步循环 + MAP-Elites + 多语言维度（核心方法）
2. 从属权利要求2-5：双层判定 + 共识筛选 + 数据回流 + 自校准PPL
3. 独立权利要求6：系统权利要求（对应方法的系统/设备）
4. 从属权利要求7-10：系统细节

**优点**：覆盖面广，一次申请保护全部创新
**缺点**：审查周期长，任何一条被驳回可能影响整体

### 方案二：两件独立专利

**第一件**：自适应攻击方法+系统（创新点1+2+4）
→ 保护攻击侧的评估方法
**第二件**：数据回流+持续改进方法+系统（创新点3+5）
→ 保护防御侧的改进闭环

**优点**：风险分散，两件独立审查
**缺点**：两件都要交费

### 关键注意事项

1. **先发表论文前先申请专利**——如果你发了论文再申请，论文会构成"现有技术"破坏新颖性
2. **准备好技术交底书**——包括系统架构图、流程图、实验数据对比表
3. **代码可以作为证据**——GitHub 仓库的时间戳可以证明优先权日
4. **审查员可能引用的对比文件**——论文本身（USENIX Security 2026）、
   PAIR/TAP/GCG 等已知攻击方法、HarmBench/SALAD-Bench 等评估框架
5. **建议请专利代理人**——权利要求的措辞直接影响保护范围

---

## 与论文的差异对照表（突出创新性）

| 技术特征 | 论文(USENIX Sec 2026) | 本发明 | 差异说明 |
|---|---|---|---|
| 进化维度 | 长度×多样性(2D) | 长度×多样性×**语言变体**(3D) | 新增语言维度 |
| 语言覆盖 | 仅英文 | **15种**中文/多语言变体 | 大幅扩展 |
| 判定机制 | LLM judge (单层) | LLM judge + **可操作性校验**(双层) | 新增假阳性过滤 |
| 种子筛选 | 直接使用基准数据集 | **共识预筛选**(多Guard交叉) | 新增筛选步骤 |
| 防御改进 | 无数据回流 | **自动化训练数据回流** | 新增闭环 |
| PPL检测 | 固定阈值6.031 | **自校准阈值**(良性×4) | 解决可移植性 |
| 防御组合 | 单独测试 | **中间件可叠加** | 新增组合能力 |
| victim多样性 | 前沿闭源API | **开源小模型矩阵**(23个) | 新增评估维度 |
