# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 实验产物（含攻击样本，绝不入库）
redteam_output*/
*.db
*.jsonl
!requirements.txt

# 模型文件（体积巨大，不入库）
*.gguf
*.safetensors

# 杂项
nohup.out
*.log
.DS_Store

# 机器本地配置（每台机器自己的，不入库；模板见 *.example）
config.yaml
remote/models.env

# 用户本地外挂词库（每台机器自己的，不入库）
detectors/wordlists/extra_*.txt
