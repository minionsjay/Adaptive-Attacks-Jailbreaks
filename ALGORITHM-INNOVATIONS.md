# 核心算法创新设计文档

## 语义边界引导的多目标自适应攻击进化方法

> 当前方法的核心缺陷：MAP-Elites 的行为描述子（文本长度、编辑距离）是**表面特征**，
> 与 Guard 的实际决策边界无关。进化只是"随机变异+筛选"，没有**学习 Guard 的弱点**。
> 本文档设计四个算法级创新，构成一个完整的改进方案。

---

## 创新点 1：语义边界引导攻击生成（Semantic Boundary-Guided Attack Generation）

### 问题

当前进化使用编辑距离衡量"多样性"，这是一个**表面特征**——两条编辑距离很远的攻击
可能在语义上完全相同（Guard 对它们的判定一致）。Guard 的决策边界在**语义空间**
中，不在编辑空间中。

### 核心思想

追踪每条攻击的**语义嵌入向量**（sentence embedding），学习 Guard 在语义空间中的
决策边界，然后在边界附近生成新攻击——那里是 Guard "犹豫"的区域，最容易突破。

### 算法

```
输入：已评估的攻击集合 A = {(x_i, blocked_i, score_i)}
      x_i = 攻击文本的语义嵌入向量 (来自 bge-small-zh 或类似模型)
      blocked_i = Guard 是否拦截

步骤 1: 边界建模
  用 A 中的 (x_i, blocked_i) 训练一个轻量分类器 f(x)
  （线性 SVM 或逻辑回归，几十个样本即可）
  f(x) 输出 Guard 对攻击 x 的"拦截置信度"

步骤 2: 边界定位
  找到使 |f(x) - 0.5| 最小的样本集 → 这些是 Guard 最"犹豫"的攻击
  即 Guard 决策边界附近的样本

步骤 3: 边界引导变异
  将边界附近的样本加入灵感组合（优先级最高）
  变异器被指示："基于这些 Guard 犹豫的攻击进行微小变异，
  保持语义不变但改变表面特征"

步骤 4: 迭代更新
  每一代评估后重新训练 f(x)，边界定位越来越准确
```

### 与现有方法的区别

| | 现有方法（编辑距离多样性） | 本方法（语义边界引导） |
|---|---|---|
| 衡量维度 | 字符级编辑距离 | 语义空间中的决策边界距离 |
| 与 Guard 关系 | 无关 | 直接建模 Guard 的决策边界 |
| 搜索效率 | 随机探索 | 沿边界定向搜索 |
| 需要的样本数 | 多（盲搜） | 少（沿边界集中搜索） |

### 专利权利要求方向

一种基于语义边界引导的 LLM 安全检测器攻击生成方法，其特征在于：
将历史攻击样本的语义嵌入向量与对应的安全检测器拦截结果输入轻量分类器，
学习所述安全检测器在语义空间中的决策边界；基于所述决策边界确定安全检测器
的判决不确定区域；在所述判决不确定区域附近生成新的攻击候选。

---

## 创新点 2：双目标进化（绕过性 × 有害性 Pareto 前沿）

### 问题

当前适应度函数将"绕过 Guard"和"越狱 victim"合并为一个标量分数，
导致两个目标被混淆。实际上它们是**独立的优化目标**：
- 绕过性强但有害性低的攻击（Guard 看不出问题，victim 也没说有害的）
- 绕过性弱但有害性高的攻击（Guard 拦了，但内容确实有害）

我们需要一个 **Pareto 前沿**：找到所有"既隐蔽又有害"的攻击，
而不是只找"最隐蔽的"（可能不有害）或"最有害的"（可能被拦）。

### 算法

```
目标 1: Bypassability(攻击) = 1 - det_score  （Guard 恶意分越低越隐蔽）
目标 2: Harmfulness(攻击) = f(victim_response)  （victim 输出越有害越高）
         f() 可以用 LLM judge 或 HarmBench 分类器

进化策略：NSGA-II 非支配排序
  - 每代按 (Bypassability, Harmfulness) 做非支配排序
  - 第一前沿 = 既不可被绕过性支配也不可被有害性支配的攻击集
  - 保留整个 Pareto 前沿（而不是单个最优解）
  - 变异器参考前沿上的攻击生成下一代

产出：
  Pareto 前沿上的攻击按用途分两类：
  - 左上角（隐蔽但轻害）→ 用于测试 Guard 的灵敏度极限
  - 右下角（显眼但重害）→ 用于测试检测器的漏报率
```

### 可视化

```
有害性
  ↑
  │  ● ●         ← 高有害+高隐蔽 = 理想攻击（最难找）
  │ ●●● ●        ← Pareto 前沿
  │ ●●●●●●
  │ ●●●●●        ← 大部分攻击落在这里
  │ ●●●          ← 低害+易检测 = 无效攻击
  └──────────────→ 绕过性（隐蔽度）
```

### 与现有方法的区别

| | 现有方法（单目标 fitness） | 本方法（双目标 Pareto） |
|---|---|---|
| 优化目标 | 单一标量 fitness | 两个独立目标 |
| 产出 | 单个"最佳"攻击 | Pareto 前沿上的攻击集 |
| 信息量 | 只知道哪个最好 | 知道隐蔽性-有害性的完整权衡关系 |
| 对防御的价值 | 只能发现一种弱点 | 能发现 Guard 在不同攻击强度下的弱点分布 |

### 专利权利要求方向

一种基于双目标非支配排序的 LLM 安全攻击进化方法，其特征在于：
将攻击候选按第一优化目标（安全检测器绕过概率）和第二优化目标
（被攻击模型输出的有害性程度）进行非支配排序，保留非支配前沿上的
攻击候选作为精英集合，所述精英集合构成隐蔽性与有害性之间的
Pareto 前沿，用于全面评估安全检测器在不同攻击隐蔽度下的防御表现。

---

## 创新点 3：课程式难度调度（Curriculum Difficulty Scheduler）

### 问题

当前进化从随机种子开始，前几代大量攻击被 Guard 拦截（浪费查询），
且攻击者模型需要多代才能"学会"绕过。

### 核心思想

模仿"课程学习"（Curriculum Learning）：从 Guard "勉强能拦住"的攻击开始
（低置信度拦截），逐步提高攻击难度。这样每一代都在 Guard 的**能力边界**
上探索，而不是浪费查询在"明显能拦住"或"明显能通过"的攻击上。

### 算法

```
输入：种子集 S, Guard G, 代数 T

初始化：
  用 S 中的种子对 G 做一次预扫描
  记录每个种子被 G 拦截的置信度 conf(s)

难度调度：
  第 1 代：选 conf(s) 最低（G 最犹豫）的种子
  第 2 代：选 conf(s) 中等的种子
  第 3 代：选 conf(s) 最高（G 最确定）的种子
  第 4+ 代：正常进化（从前面代的成功经验出发）

每代变异约束：
  变异器收到的指令随代数递增难度：
  第 1 代："做微小改变，只要 Guard 检测不到就行"
  第 2 代："在保持隐蔽的同时，增加攻击的针对性"
  第 3 代："组合多种技术，最大化攻击效果"
```

### 为什么有效

```
传统进化：  Gen1 随机攻击 → 大部分被拦（浪费）→ Gen2 随机 → ...
课程进化：  Gen1 边界攻击 → 少量被拦 → Gen2 更难 → Gen3 最难
            每一代都在 Guard 能力的边界上探索，效率更高
```

### 与现有方法的区别

| | 现有方法（随机难度） | 本方法（课程调度） |
|---|---|---|
| 攻击难度 | 随机 | 从易到难递增 |
| 信息利用 | 不区分难易 | 优先探索 Guard 边界 |
| 收敛速度 | 慢（浪费在无效攻击上） | 快（聚焦在边界上） |

---

## 创新点 4：跨 Guard 漏洞迁移（Cross-Guard Vulnerability Transfer）

### 问题

测试多个 Guard 时，每个 Guard 独立进化非常浪费——如果 Guard A 和 Guard B
对某种攻击形态都敏感，那么针对 Guard A 找到的绕过方法很可能也能绕过 Guard B。

### 核心思想

在进化攻击 Guard A 的过程中，**提取"绕过模式"**（哪些语义特征使攻击
绕过了 Guard A），然后用这个模式来**预测**哪些攻击能绕过 Guard B。
只测试预测能绕过 Guard B 的攻击（大幅减少查询数）。

### 算法

```
阶段 1: 对 Guard A 进化 → 收集绕过集 B_A = {(x_i, 特征_i)}
        提取绕过攻击的共性特征：语言变体分布、攻击类型分布、
        语义聚类中心、det_score 分布...

阶段 2: 构建迁移模型
        输入：攻击文本的语义嵌入 + Guard A 的绕过标签
        训练：轻量分类器 g(x) → "绕过 Guard A 的概率"

阶段 3: 跨 Guard 迁移
        用 g(x) 评估所有候选对 Guard B 的"可绕过概率"
        只取 top-k 个预测能绕过 Guard B 的候选去实际测试

结果：用比独立进化少 50-80% 的查询数，找到 Guard B 的绕过
```

### 效果预估

```
独立进化 Guard A: 50 次查询 → 找到 5 条绕过
独立进化 Guard B: 50 次查询 → 找到 3 条绕过
                        合计 100 次查询 → 8 条绕过

迁移方法 Guard A: 50 次查询 → 5 条绕过
迁移到 Guard B: 15 次查询（只测预测的top候选）→ 3 条绕过
                        合计 65 次查询 → 8 条绕过
                        节省 35% 查询
```

### 专利权利要求方向

一种基于跨 Guard 漏洞迁移的多安全检测器高效评估方法，其特征在于：
在针对第一安全检测器的自适应进化攻击过程中提取绕过攻击的语义特征
分布；基于所述语义特征分布训练迁移分类器；使用所述迁移分类器预测
候选攻击对第二安全检测器的绕过概率；仅对预测绕过概率超过阈值的
候选执行针对第二安全检测器的实际测试。

---

## 四个创新点的组合架构

```
┌─────────────────────────────────────────────────────────────┐
│              增强版自适应攻击进化系统                          │
│                                                             │
│  ┌─── 创新点4: 跨Guard漏洞迁移 ─────────────────────────┐   │
│  │  Guard A 的绕过模式 → 预测 Guard B 的弱点             │   │
│  └──────────────────────────────────────────────────────┘   │
│                          │                                   │
│                          ▼                                   │
│  ┌─── 创新点3: 课程式难度调度 ──────────────────────────┐   │
│  │  从 Guard 边界处开始 → 逐步提高攻击难度               │   │
│  └──────────────────────────────────────────────────────┘   │
│                          │                                   │
│                          ▼                                   │
│  ┌─── 创新点2: 双目标进化（Pareto前沿）──────────────────┐   │
│  │  同时优化 绕过性 × 有害性 → Pareto前沿                │   │
│  └──────────────────────────────────────────────────────┘   │
│                          │                                   │
│                          ▼                                   │
│  ┌─── 创新点1: 语义边界引导 ────────────────────────────┐   │
│  │  学习 Guard 的语义决策边界 → 沿边界定向搜索           │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  基础框架: MAP-Elites + 多语言维度 + 反馈闭环（已有）       │
└─────────────────────────────────────────────────────────────┘
```

---

## 与论文的对比（突出创新性）

| 技术维度 | 论文方法 | 本发明 | 创新性质 |
|---|---|---|---|
| 搜索策略 | MAP-Elites 随机变异 | **语义边界引导定向搜索** | 算法级创新 |
| 优化目标 | 单一 fitness | **双目标 Pareto 前沿** | 算法级创新 |
| 攻击难度 | 固定 | **课程式递增调度** | 策略级创新 |
| 多Guard测试 | 独立进化 | **跨Guard漏洞迁移** | 系统级创新 |
| 判定机制 | 单层 LLM judge | **双层：judge+可操作性校验** | 安全性创新 |
| 种子筛选 | 直接使用 | **共识预筛选** | 方法论创新 |
| 语言维度 | 仅英文 | **15种中文变体** | 覆盖面创新 |
| 数据回流 | 无 | **自动化闭环** | 系统级创新 |

---

## 实现优先级

| 优先级 | 创新点 | 实现难度 | 预期效果 |
|---|---|---|---|
| 1 | 语义边界引导 | 中（需 sentence-transformers） | 搜索效率提升 2-3x |
| 2 | 双目标 Pareto | 低（改 fitness 为向量+NSGA-II 排序） | 发现隐蔽性-有害性权衡 |
| 3 | 课程难度调度 | 低（改代数循环中的选择逻辑） | 早期收敛加速 |
| 4 | 跨 Guard 迁移 | 中（需训练迁移分类器） | 多 Guard 评估省 50% 查询 |

建议先实现 1+2（核心算法创新），验证有效后再加 3+4（系统优化）。
